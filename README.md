# Jogo dinossauro
Nesse projeto aprendi outro recurso do html para tag script que é a propriedade chamada defer, onde a tag script é declarada dentro de head. Com isso o javascript só é executado depois que todo o documento é carregado.
Aprendi também como capturar o evento keyup e utilizar a tecla pressionada no código.
